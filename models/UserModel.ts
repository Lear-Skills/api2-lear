export default class User {
    name : string ;
    email : string;
    userId : string;

    constructor(name : string , email : string , userId : string , ) {
        this.name = name
        this.email = email;
        this.userId = userId;
      }

}